// function() {
//   console.log("Sometimes, syntax isn't intuitive!")
// }();

// no, we have to wrap the function in parens so that it is evaluated and returns the function which can then be invoked with ()